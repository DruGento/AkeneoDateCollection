extensions:
    akeneo-attribute-date-collection-filter:
        module: pim/filter/attribute/string
        config:
            operators:
                - CONTAINS
                - DOES NOT CONTAIN
                - EMPTY
                - NOT EMPTY
